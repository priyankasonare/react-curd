steps to clone the repo
1. git clone https://github.com/your-username/pr-rec-app.git
2. cd pr-rec-app
3. npm install / npm i
4. npm run dev

git commands
1. git add .
2. git commit -m "your message"
3. git push origin main
4. git status

